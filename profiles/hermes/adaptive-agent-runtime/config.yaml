mcp_servers:
  aar:
    command: aar-hermes-mcp
    args:
      - --runtime-home
      - ${AAR_RUNTIME_HOME}
      - --route-catalog
      - ${AAR_ROUTE_CATALOG}
      - --default-route-profile
      - ${AAR_DEFAULT_ROUTE_PROFILE}
    connect_timeout: 30
    timeout: 60
